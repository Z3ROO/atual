{
  "name": "back",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon index.ts",
    "build": "npx tsc && cd ../front && npm i && npx vite build && cp -r dist ../back/ && cd ../back",
    "build-dev": "cd ../front && npx vite build && cp -r dist ../back/ && cd ../back && npx tsc",
    "start": "node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/node": "^18.13.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "typescript": "^4.9.5"
  }
}
